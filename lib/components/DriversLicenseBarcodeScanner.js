import React from 'react';
// import PropTypes from 'prop-types';
import { NativeModules, Text, requireNativeComponent } from 'react-native';

module.exports = requireNativeComponent('DriversLicenseBarcodeScanner', null);
